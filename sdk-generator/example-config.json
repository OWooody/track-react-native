{
  "clientName": "EcommerceApp",
  "packageName": "@ecommerceapp/track-sdk",
  "version": "1.0.0",
  "events": {
    "product_view": {
      "name": "product_view",
      "category": "engagement",
      "description": "Track when a user views a product",
      "requiredProperties": ["productId", "productName"],
      "optionalProperties": ["category", "price", "currency"]
    },
    "add_to_cart": {
      "name": "add_to_cart",
      "category": "conversion",
      "description": "Track when a user adds an item to cart",
      "requiredProperties": ["productId", "quantity"],
      "optionalProperties": ["price", "currency", "cartId"]
    },
    "checkout_start": {
      "name": "checkout_start",
      "category": "conversion",
      "description": "Track when a user starts checkout",
      "requiredProperties": ["cartId"],
      "optionalProperties": ["totalAmount", "currency", "itemCount"]
    },
    "payment_complete": {
      "name": "payment_complete",
      "category": "conversion",
      "description": "Track when a payment is completed",
      "requiredProperties": ["orderId", "amount", "currency"],
      "optionalProperties": ["paymentMethod", "items"]
    }
  }
} 